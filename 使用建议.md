# 小乖的考公错题屋 · 使用建议文档

> 版本：v0.2.0 配套文档 · 适用对象：个人备考用户（Deca）
> 核心理念：**错题不是终点，是复习的起点** —— 从收集到复查形成闭环，让每一道错题都变成知识点资产。

---

## 一、整体理念：一条学习闭环

这个 App 不是四个独立功能，而是一条**接力流水线**：

```
① 收集 ──→ ② 消化 ──→ ③ 沉淀 ──→ ④ 复查
  ↑                                      │
  └────── 复查结果回填（闭环）←──────────┘

① 收集：拖拽/粘贴/批量导入错题截图（自动去重，可选自动分类）
② 消化：AI 分类 → 待确认流水线逐题核对 → 入库 + 复盘
③ 沉淀：生成每日笔记 + 知识卡片，薄弱点清单更新
④ 复查：加权复习队列 → 组卷重做 → 对答案回填 → 错的回到 ①
```

**核心机制**：复查结果必须回填——做错的题错误计数 +1、对应知识卡片权重提升、回到复习队列。系统会"记住"你反复错的点，越复习越精准。

首页顶部的**流程状态条**（收集 → 确认 → 归纳 → 复习）就是这条闭环的仪表盘：哪个环节有积压，一目了然，点击直达。

---

## 二、推荐每日工作流（SOP）

建议每天固定一个时段（如晚间）完成，全程约 20~40 分钟：

| 步骤 | 操作 | 耗时 | 页面 |
|------|------|------|------|
| 1. 收集 | 把当天错题截图**一次性拖入**首页（多选/粘贴均可） | 1~2 分钟 | 成品库 |
| 2. 分类 | 开启「导入后自动分类」则无需操作；否则全选 → 批量 AI 分类 | 3~8 秒/张（并行） | 成品库 |
| 3. 确认 | 进入待确认，开启**自动播放**，逐题核对后按 Enter 确认 | 5~10 秒/题 | 待确认 |
| 4. 复盘 | 难题点「AI 生成」复盘思路；图形题裁剪标记 | 可选 | 待确认 |
| 5. 沉淀 | 确认完弹出提示 → 一键「归纳今日知识点」 | 1~2 分钟 | 知识归纳 |
| 6. 复习 | 查看今日复习队列（5 张），逐个「已复习」或「做错了」 | 5 分钟 | 知识归纳 |
| 7. 抽查 | 周末用「从复习队列选题」生成组卷重做，做完「对答案回填」 | 30 分钟 | 组卷导出 |

> 说明：确认环节**本来就该逐题**——因为需要人眼核对 AI 结果。批量导入 + 自动分类解决"灌入"的批量问题，自动播放 + 快捷键解决"确认"的流水线问题。

---

## 三、分模块使用建议

### 3.1 成品库（收集）

- **批量导入**：一次拖入多张截图；同一张图重复导入会被自动跳过（按内容识别）
- **粘贴**：任意位置 Cmd+V / Ctrl+V 直接粘贴剪贴板截图
- **建议开启**「设置 → 学习流程 → 导入后自动分类」，导入完直接进待确认，省两步操作
- **编辑裁剪**：每张图可「编辑」→ 先旋转（左/右 90°）再拖拽框选；框选**可以拖到图片边缘**，不会中断（坐标已钳制）
- 已分类未确认的图会显示 AI 分类标签和置信度，点「去确认」直达

### 3.2 待确认（消化）

**快捷键（核心体验）**：

| 按键 | 功能 |
|------|------|
| `⌘Enter` 或 `Ctrl+Enter` | 确认入库（永远可用） |
| `Enter`（自动播放开启时） | 确认并自动进入下一题 |
| `⌘S` | 跳过（回到待分类） |
| `←` / `→` | 上一题 / 下一题 |

- **自动播放**：打开后单手按 Enter 即可连续处理，输入框内按 Enter 不会误触发
- **低置信度**：标签区显示橙色警示，重点核对；阈值可在设置调整
- **未知分类（黄色虚线 chip）**：点开三选一——
  - 归入已有分类：选树中节点，直接映射
  - **采纳为新分类**：选父节点后写入分类树（带 AI 标记），以后 AI 会优先返回它
  - 标记其他：归入"其他"
- **图形题**：AI 检测到图形区域时可「裁剪图形区域」只保留图、或「整张作为图形」；组卷时图形题可只显示图形
- **复盘**：难题点「AI 生成」拿到解题思路 + 应试技巧，会随笔记写入

### 3.3 知识归纳（沉淀）

- 每天确认完错题后点「归纳今日知识点」：生成**每日笔记**（今日概览/错题分布/新知识点/踩坑回顾）+ 更新**知识卡片**
- 卡片四类：易错点（pitfall）/ 概念辨析 / 速解技巧 / 公式定理
- 卡片详情里能看到**关联错题**（同知识点的错题列表），点击可跳 Obsidian
- 打开 App 会自动补跑最近 7 天未归纳的日期

### 3.4 复习（复查）—— 重点推荐

- **今日复习队列**：按 错误次数×3 + 距上次复习天数 + 易错点加成 加权排序，每天推荐 5 张（数量可设置），已复习的不重复出现
- 看完卡片后两个动作：
  - **「已复习」**：记录复习日志，今天不再出现
  - **「做错了」**：卡片错误计数 +1，权重提升，之后更频繁出现
- **组卷重做闭环**：组卷页 →「从复习队列选题」一键生成薄弱点试卷 → 导出打印 → 重做 →「对答案回填」勾选错题 → 系统把做错的题错误计数 +1 并同步到知识卡片和 Obsidian 笔记
- 选题顺序 = 试卷顺序：右侧列表可用 ↑↓ 调整；「按错误降序」一键重排；导出前有预览确认

### 3.5 统计看板

- 8 项指标：总错题数 / 今日新增 / 本周（含趋势箭头）/ 本月 / 日均入库 / 高频题型 / 薄弱预警 / 待确认
- **高频错题 TOP 表**每行有「组卷」按钮 → 直接生成该题型的重做卷
- 趋势图支持 7/30/90 天及自定义区间；错误类型分布能看到自己"为什么错"（粗心 vs 知识点）
- 「AI 分析建议」：一键生成个性化备考建议

### 3.6 错题库

- 列表 / **按知识点分组** 两种视图切换；分组视图按累计错误排序，展开可见每题，可一键组卷重做
- 编辑题目分类后，Obsidian 里的笔记会自动迁移到新目录
- 校验 / 重排目录：用于修复 vault 与数据库不一致

### 3.7 标签管理

- 维护三级分类树（增删改、**拖拽排序**、导入导出 JSON）
- AI 采纳的新分类带绿色「AI」角标，可识别哪些是系统自生长的
- 错误类型建议维护成 5~8 个高频词（如：粗心大意 / 知识点未掌握 / 方法不当 / 计算错误），统计才有意义

---

## 四、设置项推荐配置

| 设置项 | 推荐值 | 说明 |
|--------|--------|------|
| 本地数据目录 | `~/考公错题`（默认） | 错题 Markdown 和图片的本地存储 |
| EasyImage 图床 | 可选 | 配了图片用 NAS URL，不配用本地路径 |
| Obsidian Vault | 必配 | 所有笔记/卡片写这里 |
| 导入后自动分类 | **开启** | 批量导入后直接进待确认 |
| AI 流水线 | 两阶段 | 视觉识别 + 推理分类，更准 |
| 置信度阈值 | 0.7 | 低于此值确认页高亮 |
| 弱点提醒阈值 | 5 | 累计 5 次以上标红 |
| 每日复习数量 | 5 | 3~8 均可，别贪多 |
| 快速入库默认错误次数 | 1 | 快速入库时默认值 |
| 上传并发数 | 5 | 导入速度快与稳定的平衡 |
| 压缩阈值/质量 | 500KB / 0.7 | 截图一般不会被压，原图保留 |
| 日志级别 / 保留 | WARN / 30 天 | 出问题再调 DEBUG |

---

## 五、数据与安全

- **数据分布**：
  - 结构化数据 → 本地 SQLite（`~/Library/Application Support/wrong-answer-app/data.db`）
  - 错题 Markdown + 图片 → 本地数据目录（默认 `~/考公错题`）
  - 笔记/知识卡片 → Obsidian Vault
- **API Key 安全**：使用系统钥匙串加密存储（safeStorage），日志中不落明文
- **离线可用**：Obsidian 写失败自动进重试队列（首页「待补写」卡片可手动刷新），恢复后自动补写
- **日志排查**：出问题时看「日志」页，按 trace_id 追踪单张错题的完整链路，可导出分享
- **备份建议**（重要）：定期备份三处——Obsidian Vault、本地数据目录、data.db（复制文件即可，App 关闭时备份最稳妥）

---

## 六、常见问题（FAQ）

**Q1：待确认队列重启 App 后还在吗？**
在。AI 结果已持久化，重启后自动恢复待确认队列。

**Q2：Obsidian 笔记没写进去怎么办？**
Vault 写失败会进入重试队列并自动补写；首页「待补写」卡片可点击立即重试。确认 Vault 路径配置正确且目录存在。

**Q3：复习队列为什么是空的？**
需要先有知识卡片——确认错题后生成过「今日知识点归纳」才会有卡片；新卡片权重低，可能排在后面。

**Q4：组卷导出时提示图片下载失败？**
检查网络；EasyImage 地址不可达时会回退本地图。PDF 会正常生成，只是缺个别图。

**Q5：自动归纳没跑？**
启动时会自动补跑近 7 天；当天确认完会弹提示手动点一次更即时。

**Q6：分类树可以随便改吗？**
可以，完全自定义。但删改会影响已有错题的标签——删除节点不会删错题，只是标签不再有对应节点。

**Q7：如何迁移到新电脑？**
拷贝三处：`~/考公错题`、Vault、data.db，装好 App 后在设置里重新指向 Vault 路径即可。

---

## 七、两种使用节奏

### 轻量模式（每天 3~10 题）
- 开启「导入后自动分类」+ 自动播放
- 每天只做：拖入 → 确认 → 归纳 → 5 张复习，全程 15 分钟
- 不追求当天全部确认，积压的会自动恢复

### 冲刺模式（刷题高峰期，每天 30+ 题）
- 批量拖入 → 自动分类 → 待确认流水线连按 Enter
- 低置信度题重点核对，其余快速确认
- 周末固定「从复习队列选题」生成专项卷重做 + 回填
- 用统计看板盯"薄弱预警"和"错误类型分布"调整复习优先级

---

*愿每一道错题都变成你上岸路上的一块砖。*
